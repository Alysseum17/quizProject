# Вклад учасників (Contributors)

Проєкт розроблено командою студентів. Нижче наведено список учасників та їхній вклад у розробку.

## 👥 Команда:

## Учасник 1: Марченко Данііл
* Виконана робота:
- Реалізував повний функціонал для модуля Quiz (Schema, Router, Controller, Service).
- Реалізував функціонал проходження квізу(від початку спроби до підтвердження та результами)
- Реалізував повний функціонал для модуля User (Schema, Router, Controller, Service).
- Реалізував повний функціонал для автнентифікації(Schema, Router, Controller, Service), використовуючи mail сервіс.
- Написав middleware для глобального error handling та написав декілька утиліт(кастомні помилки та кастомний реквест)
- Впровадив транзакційне створення квізу одразу з питаннями та варіантами відповідей
- В своїх реалізованих модулях реалізував багато аналітичних запитів(списко найкращих авторів по різним критеріям, список найкращих користувачів, пошук квізів з сортуванням по рейтингу, даті створення)
- Написав інтеграційні тести для модуля Quiz (quiz.test.ts), модуля Auth (auth.test.ts) та модуля User (user.test.ts)
- Написав модульні тести для автентифікації, окремо для одного з найважливіших middleware protect та модульні тести для schemas
- Задеплоїв все в докер з варіантом для тестування
- Зробив README.md
  

## Учасник 2: Жила Іван
* Виконана робота:
- Реалізував повний функціонал для модуля Reviews (Schema, Router, Controller, Service).
- Реалізував повний функціонал для модуля Question (Schema, Router, Controller, Service).
- Впровадив транзакційне створення питань разом із варіантами відповідей для забезпечення цілісності даних.
- Написав складний аналітичний SQL-запит для отримання статистики відгуків (гістограма оцінок, відсоток позитивних) через prisma.$queryRaw.
- Написав інтеграційні тести для модуля Reviews (review.test.ts) та сценаріїв управління питаннями в quiz.test.ts.

## Учасник 3: Семченко Ілля
* Виконана робота:
  
* Реалізував повний функціонал для модуля **Bookmarks** (Schema, Router, Controller, Service).
* Впровадив складні транзакційні операції для роботи з даними: масове додавання закладок (`bulkAdd`) та "розумна" очистка неактивних записів (`cleanup`).
* Розробив функціонал детального управління варіантами відповідей (`AnswerOption`): можливість додавати, редагувати та видаляти окремі відповіді з перевіркою прав доступу.
* Написав оптимізований Raw SQL запит для аналітики найпопулярніших квізів у закладках.
* Покрив реалізований функціонал розширеними інтеграційними тестами (`bookmark.test.ts`, `answers.test.ts`).  





## Зробили разом, спілкуючись онлайн:
- Зробили стартовий сетап проєкту(все встановили, написали prisma schema, все налаштували)
- Зробили документацію(для schema та query)
